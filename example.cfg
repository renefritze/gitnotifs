[general]
repo_path=/tmp/gitnotifs
active=rss
link=https://github.com/renemilk/gitnotifs/compare/{{old_rev}}...{{new_rev}}
body={{link}}

    {% for d in diffs -%}
        {{d.filename}} ({{d.cat}})
    {% endfor %}

    {% for d in diffs -%}
        {{d.diff}}
    {% endfor %}

[mail]
recipients=rene@milk.pm

[jabber]
recipients=somedue@example.net
body={{statstring}}
    {{link}}

[facebook]
recipients=Some Dude
Another Dude
body={{link}}

[IRC]
body={% for d in diffs -%}
        {{d.filename}} ({{d.cat}})
    {% endfor %}
    {{link}}

[rss]
file=/tmp/feed.xml
title=gitnotifs commit feed
description=gitnotifs commit feed
body=<h2><a href="{{link}}">GitHub</a></h2>
		{{shortlog}}
    <ul>{% for d in diffs -%}
        <li>{{d.filename}} ({{d.cat}})</li>
    {% endfor %}</ul>

    {% for d in diffs -%}
        {{d.diff}}
    {% endfor %}