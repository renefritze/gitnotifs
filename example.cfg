[general]
repo_path=/tmp/gitnotifs
active=rss
body=https://github.com/renemilk/gitnotifs/compare/{{old_rev}}...{{new_rev}}

    {% for d in diffs -%}
        {{d.filename}} ({{d.cat}})
    {% endfor %}

    {% for d in diffs -%}
        {{d.diff}}
    {% endfor %}

[mail]
recipients=rene@milk.pm

[jabber]
recipients=somedue@example.net
body={{statstring}}
    https://github.com/renemilk/gitnotifs/compare/{{old_rev}}...{{new_rev}}

[facebook]
recipients=Some Dude
body=https://github.com/renemilk/gitnotifs/compare/{{old_rev}}...{{new_rev}}

[IRC]
body={% for d in diffs -%}
        {{d.filename}} ({{d.cat}})
    {% endfor %}
    https://github.com/renemilk/gitnotifs/compare/{{old_rev}}...{{new_rev}}

[rss]
file=/tmp/feed.xml
title=gitnotifs commit feed
description=gitnotifs commit feed
body=<h2><a href="https://github.com/renemilk/gitnotifs/compare/{{old_rev}}...{{new_rev}}">GitHub</a></h2>
		{{shortlog}}
    <ul>{% for d in diffs -%}
        <li>{{d.filename}} ({{d.cat}})</li>
    {% endfor %}</ul>

    {% for d in diffs -%}
        {{d.diff}}
    {% endfor %}